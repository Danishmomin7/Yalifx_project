{
  "buildCommand": "pip install -r requirements.txt",
  "devCommand": "uvicorn app.main:app --reload",
  "env": {
    "PYTHONUNBUFFERED": "1"
  },
  "functions": {
    "api/index.py": {
      "maxDuration": 30
    }
  }
}
